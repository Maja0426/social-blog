<% include ../partials/header %>

<div class="container">

  <div class="row mb-5">
    <div class="col-12">
    <table class="table table-sm table-striped">
      <thead class="">
        <tr>
          <th>Cikk közzététele</th>
          <th>Cikk szerzője</th>
          <th>Cikk címe</th>
          <th>Ugrás a cikkhez</th>
        </tr>
      </thead>
      <tbody>
        <% allContent.forEach(function(content){ %>
        <tr>
          <td>
            <%= moment(content.createdAt).format('YYYY. MM D.') %>
          </td>
          <td>
            <%= content.author.username %>
          </td>
          <td>
            <%= content.title %>
          </td>
          <td class="float-right">
            <a class="btn btn-sm btn-secondary" href="/contents/<%= content._id %>"><i class="far fa-eye"></i> Megnézem</a>
          </td>
        </tr>
        <% }) %>
      </tbody>
    </table>
    </div>
  </div>
</div>

<% include ../partials/footer %>